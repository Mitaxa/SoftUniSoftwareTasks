function amazingNumbers(num) {
    let numToString = String(num);
    let result = 0;

    for (let i = 0; i < numToString.length; i++) {
        let num = Number(numToString[i]);
        result += num;
    }
    for (let i = 0; i < resultToString)
    console.log(result);
}
amazingNumbers(1233)